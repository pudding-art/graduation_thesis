git clone git@github.com:pudding-art/graduation_thesis.git
git remote //get messages about remote repo

git remote -v

git branch //only master message could be explicit

//if you want to use dev branch to develop
git switch -c dev origin/dev
git checkout -b dev origin/dev

git add env.txt
git commit -m "add env"

git push origin dev
